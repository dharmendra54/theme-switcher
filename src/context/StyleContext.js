import React,{createContext} from "react";

const PackageContext=createContext()

export default PackageContext;

// export default createContext();
















//Context
//context help us to create global variable in react.
//It means same variable can be accessed in multiple components.

//Theme-switcher
//A theme switcher allows user to switch between different themes such as light and dark modes to enhance their user experience.

//styleContext.js
//use one function called createContext.
//It creates global object.

